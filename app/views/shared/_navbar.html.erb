<div class="navbar border-bottom shadow p-3 bg-body-tertiary text-center bg-dark">
  <%= link_to 'YouCrime', crimes_path, style: 'text-decoration: none; color: black;' %>
  <% unless (current_user == nil) %>
    <%= link_to 'Login', new_user_session_path, class: 'btn btn-white' %>
  <% end %>
</div>
